# EAS uses this instead of .gitignore — keep aligned with repo ignores.
# Never upload local native projects (Windows SDK paths, stale Gradle config).
android/
ios/
**/local.properties

# Repo-wide (archive root is the monorepo)
node_modules/
.expo/
dist/
.next/
out/
apps/web/out/
.turbo/
*.local
.env
.env*.local
!.env.example
.DS_Store
*.pem
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/
apps/mobile/dist-web/
coverage/
.vercel
supabase/.temp/
*.log
package-lock.json
yarn.lock
npm-shrinkwrap.json

# Mobile-only
.kotlin/
*.tsbuildinfo
expo-env.d.ts
